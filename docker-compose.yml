services:
  ambiente-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: runner-dev
    # Espelha a pasta atual do teu computador para a pasta /app dentro do container
    volumes:
      - .:/app